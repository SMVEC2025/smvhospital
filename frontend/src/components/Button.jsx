import React from 'react';
import '../styles/Button.css';

function Button({name}) {
    return (
        <button class="button">
          {name}
        </button>
            );
        }
        
 
export default Button;
